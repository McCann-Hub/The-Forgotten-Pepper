@import './headerDropdown.styl';
@import './socialBar.styl';

#global-header {
    @apply: bg-gray-200 bg-opacity-75 text-gray-800;
}

.dark {
    #global-header {
        @apply: bg-gray-800 bg-opacity-75 text-gray-200;
    }
}